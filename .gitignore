# کلیدها — هرگز در گیت commit نکن
secrets.env

# macOS
.DS_Store
__MACOSX/

# Node
node_modules/

# Local tooling config (machine-specific)
.claude/

# Bridge runtime state (active model selection)
model-state.json

# Dashboard runtime state (projects, chat sessions, board cache)
data/
